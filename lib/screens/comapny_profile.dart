import 'package:flutter/material.dart';

class CompanyProfile extends StatelessWidget {
  const CompanyProfile({super.key, this.tickerSymbol});

  final String? tickerSymbol;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
